<header>
    <a id="logo" href="{{@website.url}}">
        {{#if @website.logo}}
            <img src="{{@website.logo}}" alt="{{@website.name}}">
        {{else}}
            {{@website.name}}
        {{/if}}
    </a>
    
    {{#if menus.mainMenu}}
        {{> menu menus.mainMenu}}
    {{/if}}
    
    {{{@customSearchInput}}}
</header>